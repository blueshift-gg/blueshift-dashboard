import { ArticleSection } from "../../../../components/ArticleSection/ArticleSection";

# Ferramentas

Escrever e compilar programas em sBPF Assembly tradicionalmente exigia a toolchain completa do Solana, que pode ser complexa de configurar e dependente da plataforma. Por esse exato motivo, [Dean](https://x.com/deanmlittle), com contribuição de [Claire](https://x.com/clairefxyz), criou o sBPF: um kit de ferramentas completo para criar e compilar programas em sBPF Assembly.

<ArticleSection name="Instalação" id="instalacao" level="h2" />

Começamos instalando o pacote executando:

```
cargo install --git https://github.com/blueshift-gg/sbpf.git
```

Depois disso, poderemos executar todos os seguintes comandos:

- `init`: Criar um scaffold de novo projeto.
- `build`: Compilar em um executável de programa Solana.
- `deploy`: Compilar e implantar o programa.
- `test`: Testar o programa implantado.
- `e2e`: Compilar, implantar e testar um programa.
- `clean`: Limpar artefatos de compilação e implantação.
- `help`: Exibir esta mensagem ou a ajuda do(s) subcomando(s) fornecido(s).

```text
Usage: sbpf <COMMAND>

Commands:
  init    Create a new project scaffold
  build   Compile into a Solana program executable
  deploy  Build and deploy the program
  test    Test deployed program
  e2e     Build, deploy and test a program
  clean   Clean up build and deploy artifacts
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help     Print help
  -V, --version  Print version
```

<ArticleSection name="Inicializar um novo projeto" id="inicializar-um-novo-projeto" level="h2" />

Para criar um novo projeto, use o comando `sbpf init`. Por padrão, ele inicializa um projeto com testes em Rust usando o Mollusk. Você também pode inicializar um projeto com testes em TypeScript usando a opção `--ts-tests`.

```text
sbpf init --help
Create a new project scaffold

Usage: sbpf init [OPTIONS] [NAME]

Arguments:
  [NAME]  The name of the project to create

Options:
  -t, --ts-tests  Initialize with TypeScript tests instead of Mollusk Rust tests
  -h, --help      Print help information
  -V, --version   Print version information
```

Após inicializar o projeto, você pode navegar até o diretório do projeto e usar outros comandos para compilar, implantar e testar seu programa.

<ArticleSection name="Compilando Programas" id="compilando-programas" level="h2" />

O sBPF fornece duas opções de compilação, dependendo da sua configuração e necessidades:

### Compilação sBPF

O comando de compilação usa um assembler independente que não requer dependências adicionais. É perfeito para aprendizado e experimentação, pois "simplesmente funciona" em qualquer plataforma.

```
sbpf build
```

{/* TODO: Atualizar sbpf para habilitar compilação llvm como configuração opcional e então reativar isto

### Full Build

O comando de compilação padrão fornece integração com a toolchain completa do Solana e recursos de gerenciamento de projetos.

> Isso requer que as [Ferramentas de Linha de Comando do Solana](https://docs.anza.xyz/cli/install/) estejam instaladas.

````
sbpf build
``` */}

### Uso Avançado

Você pode substituir o linker padrão por um arquivo de linker personalizado, incluindo-o no diretório src com o mesmo nome do seu programa. Por exemplo:

```text
src/example/example.s
src/example/example.ld
````

<ArticleSection name="Ambiente de Desenvolvimento" id="ambiente-de-desenvolvimento" level="h2" />

Para a melhor experiência de desenvolvimento, considere instalar a [extensão VS Code sBPF Assembly](https://github.com/deanmlittle/vscode-sbpf-asm) que fornece:

- Destaque de sintaxe para assembly sBPF
- Autocompletar instruções e registradores
- Detecção de erros e avisos
