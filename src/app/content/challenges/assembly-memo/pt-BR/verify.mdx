import { ChallengeTitle } from "../../../../components/Challenges/ChallengeTitle";
import { RequirementList } from "../../../../components/Challenges/RequirementList";
import { Requirement } from "../../../../components/Challenges/Requirement";

<ChallengeTitle>sBPF Assembly Memo</ChallengeTitle>

Crie um programa que tenha apenas uma instrução. Ela deve:

- Deve falhar se os dados da instrução contiverem quaisquer contas (isso pode ser feito definindo `r0` para o número de contas)
- Definir `r2` para o comprimento dos dados da instrução, e `r1` para o offset dos dados da instrução,
- Invocar a syscall `sol_log_`.

Comece instalando o pacote sBPF como explicado na [seção de ferramentas](/pt-BR/courses/introduction-to-assembly/tooling) do curso de Introdução ao Assembly e compile seu programa usando o seguinte comando no seu terminal:

```
sbpf build
```

Isso gerará um arquivo `.so` na sua pasta `target/deploy`.

Agora clique no botão `take challenge` e solte o arquivo lá!

<RequirementList>
  <Requirement title="Desafio 1: Testando o Memo">
    Seu programa deve ter sucesso e registrar `Hello, Solana!`
  </Requirement>

  <Requirement title="Desafio 2: Testando o Memo com Entrada Diferente">
    Para garantir que você não colocou a string `Hello, Solana!` fixa no código, tentamos novamente com outra entrada: `Hello, Solana, but different!`
  </Requirement>
</RequirementList>
