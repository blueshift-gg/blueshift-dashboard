import { ArticleSection } from "../../../../../components/ArticleSection/ArticleSection";

<ArticleSection name="Conclusion" id="conclusion" level="h2" />

You can now test your program against our unit tests and claim your NFT!

Start by building your program using the following command in your terminal:

```bash
anchor build
```

This generates a `.so` file directly in your `target/deploy` folder.

Now click on the **Take Challenge** button and upload the file there!
